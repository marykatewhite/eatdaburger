<div class="jumbotron-fluid">
<img id="burgerpic" src="http://www.pngmart.com/files/1/Yummy-Burger-PNG.png">
<h1>EAT-DA-BURGER</h1>
</div>

<h2>Not devoured:</h2>
<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burger-block devoured=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Devoured burgers:</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burger-block devoured=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form" id="burgerform">

  <div class="form-group">
    <label for="newBurger">Your new burger:</label>
    <input type="text" id="newBurger" name="name">
  </div>

  <button id="burgerbutton" type="submit">Make-da-Burger</button>
</form>

