<div class="jumbotron">
  <div class="container">
    <img id="burgerpic" src="http://www.pngmart.com/files/1/Yummy-Burger-PNG.png">
    <h1>EAT-DA-BURGER</h1>
  </div>
</div>

<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-6" id="notDevoured">
      <h2>Not devoured:</h2>

      <ul>
        {{#each burgers}}
        {{#unless devoured}}
        {{> burger-block devoured=false}}
        {{/unless}}
        {{/each}}
      </ul>
    </div>

    <div class="col-sm-12 col-md-6" id="devoured">
      <h2>Devoured burgers:</h2>

      <ul>
        {{#each burgers}}
        {{#if devoured}}
        {{> burger-block devoured=true}}
        {{/if}}
        {{/each}}
      </ul>
    </div>
  </div>
</div>

<div class="container" id="addBurgerContainer">
  <div class="row">
    <div id="addBurger" class="col-12">
      <h2>Add-Da-Burger</h2>
      <form class="create-form" id="burgerform">
        <div class="form-group">
          <input type="text" class="form-control" id="newBurger" placeholder="Type in your perfect burger...">
        </div>
        <button type="submit" class="btn btn-lg btn-secondary" id="makeDaBurger">Make-da-Burger</button>
      </form>
    </div>
  </div>
</div>

<footer class="footer">
  <div class="container">
    <p id="lilnote">*I made this footer just so my page would look like a burger. This is the bottom bun.</p>
  </div>
</footer>